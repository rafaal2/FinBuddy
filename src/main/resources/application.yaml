spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/financeApi?useTimezone=true&serverTimezone=UTC
    username: root
    password: ADS2023
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
      show-sql: false
logging:
  level:
    org:
      springframework=DEBUG:

